<Window x:Class="ShadowrunCombatHelper.UserControls.InitiativeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShadowrunCombatHelper.UserControls"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="Enter Initiative Roll" Height="126.761" Width="570.563">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Name="InitiativeLabel" Grid.Row="0"/>
        <TextBox Name="InitiativeInput" Grid.Row="1" KeyDown="InitiativeInput_KeyDown"/>
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center">
            <Button Name="btnOK" Width="100" Margin="0,0,35,0" Click="btnOK_Click">OK</Button>
            <Button Name="btnCancel" Width="100" Margin="35,0,0,0" Click="btnCancel_Click">Cancel</Button>
        </StackPanel>

    </Grid>
</Window>
