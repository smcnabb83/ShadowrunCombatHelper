<Page x:Class="ShadowrunCombatHelper.Views.InitiativeTracker_View"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ShadowrunCombatHelper.Views"
      xmlns:viewmodels="clr-namespace:ShadowrunCombatHelper.ViewModels"
      xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
      xmlns:charsummary="clr-namespace:ShadowrunCombatHelper.UserControls"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="InitiativeTracker_View">
    <Page.DataContext>
        <viewmodels:InitiativeTracker_ViewModel/>
    </Page.DataContext>
    <Page.Resources>
        <CollectionViewSource x:Key="CombatantSource" Source="{Binding CombatQueue}">
            <CollectionViewSource.SortDescriptions>
                <scm:SortDescription PropertyName="Initiative" Direction="Descending"/>
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>

        
        <!--<DataGrid Grid.Column="1" ItemsSource="{Binding Source={StaticResource CombatantSource}}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <charsummary:CharacterSummaryDisplay/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Name" Binding="{Binding CharacterName}"/>
                <DataGridTextColumn Header="Affiliation" Binding="{Binding Affiliation}"/>
                <DataGridTextColumn Header="Initiative" Binding="{Binding Initiative}"/>
            </DataGrid.Columns>
        </DataGrid>-->

        <ItemsControl x:Name="CharacterStack" Grid.Column="1" ItemsSource="{Binding Source={StaticResource CombatantSource}}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <charsummary:CharacterSummaryDisplay/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        
        <StackPanel Orientation="Vertical">
            <TextBlock Text="{Binding Source = {StaticResource CombatantSource}, Path=CharacterName}"/>
            <TextBlock Text="{Binding  Source = {StaticResource CombatantSource}, Path=CharStatus}"/>
        </StackPanel>

    </Grid>
</Page>
